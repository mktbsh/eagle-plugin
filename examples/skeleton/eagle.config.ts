import { defineConfig } from "eagle-plugin";

export default defineConfig({
  id: "eagle-plugin-skeleton",
  version: "0.0.0",
  name: "Eagle Plugin Skeleton",
  logo: "logo.svg",
  keywords: [],
  window: {
    width: 640,
    height: 480,
  },
});
